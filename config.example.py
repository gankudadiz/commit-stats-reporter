# -*- coding: utf-8 -*-
"""
代码统计工具配置文件示例

使用方法：
    cp config.example.py config.py

然后编辑 config.py，填写你自己的项目路径和提交者过滤条件（可选）。
注意：config.py 已被加入 .gitignore，不会提交到版本库。
"""

# ============================================================
# 系统语言设置 (System Language Setting)
# ============================================================
# 支持的语言 / Supported languages / サポートされている言語: 
#   'zh' - 简体中文 (默认/Default)
#   'en' - English
#   'ja' - 日本語
LANGUAGE = "zh"


# ============================================================
# 项目配置列表
# ============================================================
# 每个项目是一个字典，包含以下字段：
#   name  - 项目名称（将显示在报告中）
#   path  - 项目在本地的绝对路径
#   type  - 版本控制系统类型，可选值：'git' 或 'svn'
#
# 同一个目录下可同时存在 Git 和 SVN 两套记录，分别配置即可。
PROJECTS = [
    # Git 项目示例
    {"name": "MyProject", "path": "/path/to/your/project", "type": "git"},

    # SVN 项目示例
    {"name": "LegacyApp", "path": "/path/to/your/svn/project", "type": "svn"},

    # 同一项目同时有 Git 和 SVN 仓库时，可以分别配置
    # {"name": "ProjectA-Git", "path": "/path/to/project_a", "type": "git"},
    # {"name": "ProjectA-SVN", "path": "/path/to/project_a_svn", "type": "svn"},
]

# ============================================================
# 输出目录
# ============================================================
# 生成的 Excel / HTML 报告将保存到此目录（相对路径或绝对路径均可）
OUTPUT_DIR = "output"

# ============================================================
# 默认统计时间范围（可选）
# ============================================================
# 格式：'YYYY-MM-DD:YYYY-MM-DD'
# 留空则在交互模式下让用户选择，或统计全部历史记录
DATE_RANGE = ""

# ============================================================
# 默认输出文件名（可选）
# ============================================================
# Excel 文件名
EXCEL_FILENAME = "代码统计报告.xlsx"

# HTML 文件名
HTML_FILENAME = "代码统计报告.html"

# ============================================================
# 自定义作者名（可选）
# ============================================================
# 填写后，报告中所有记录的作者将统一显示为该名称。
# 留空则使用 git log / svn log 中真实的提交者名称。
# Git 作者名为 git log 中显示的 author name；
# SVN 作者名为 svn log 中显示的用户名。
AUTHOR_NAME = ""  # 示例："zhangsan"
